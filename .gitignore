# NIKDY necommitovat hesla a tokeny
.streamlit/secrets.toml

# lokální cache nahraných dat — trvale se zrcadlí do Supabase, NE do repa.
# Repo je veřejné, takže faktury sem nesmí (proto ignorujeme).
data_cache/

# python smetí
__pycache__/
*.pyc
.DS_Store
